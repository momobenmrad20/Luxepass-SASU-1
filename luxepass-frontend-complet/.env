VITE_API_BASE=http://localhost:4000
# Clé PUBLIQUE Stripe (pk_test_… / pk_live_…) — Dashboard Stripe > Développeurs
# > Clés API. Jamais la clé secrète ici (elle reste côté backend, .env du
# luxepass-backend). Vide = StripeCheckoutForm affiche un message d'erreur
# au lieu du formulaire de carte, cf. StripeCheckoutForm.jsx.
VITE_STRIPE_PUBLISHABLE_KEY=
