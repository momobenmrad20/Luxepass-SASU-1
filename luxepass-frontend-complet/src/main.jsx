import React from "react";
import ReactDOM from "react-dom/client";
import LuxePassApp from "./LuxePass.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LuxePassApp />
  </React.StrictMode>
);
