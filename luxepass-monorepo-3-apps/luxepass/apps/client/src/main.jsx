import React from "react";
import ReactDOM from "react-dom/client";
import ClientApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClientApp />
  </React.StrictMode>
);
